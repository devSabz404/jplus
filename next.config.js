/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  //target: "serverless",
  images: {
    domains: ['jendieplus.co.ke'],
  },
  
  
  
 
}

module.exports = nextConfig
